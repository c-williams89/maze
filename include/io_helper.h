#ifndef IO_HELPER_H
#define IO_HELPER_H
	// typedef struct graph_data {
	//      uint16_t rows;
	//      uint16_t cols;
	// } graph_data;
int validate_file(FILE * fp);
	// graph_data * get_graph_size(FILE *fp);
#endif
